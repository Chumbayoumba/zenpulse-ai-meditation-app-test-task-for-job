EXPO_PUBLIC_AI_ENDPOINT=
# Optional: override the default AI route with your own server endpoint.
# In development, the app will auto-try http://<expo-host>:8788/api/affirmation first
# when the Expo host is localhost / LAN and the local proxy is running.
